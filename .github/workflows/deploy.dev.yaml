name: deploy dev

on:
    workflow_dispatch:
    push:
        branches:
            - main

env:
    REGISTRY_NAME: dscrdusceacrdev

jobs:
    build:
        runs-on: ubuntu-latest
        environment: dev
        steps:
            -   name: Checkout Repository
                uses: actions/checkout@v2

            -   name: Login to Azure Container Registry
                uses: docker/login-action@v1
                with:
                    registry: ${{ env.REGISTRY_NAME }}.azurecr.io
                    username: ${{ env.REGISTRY_NAME }}
                    password: ${{ secrets.CONTAINER_REGISTRY_PASSWORD }}

            -   name: Set up Docker Buildx
                uses: docker/setup-buildx-action@v1

            -   name: Build and Push
                uses: docker/build-push-action@v2
                with:
                    builder: ${{ steps.buildx.outputs.name }}
                    context: ./src
                    file: ./src/Seraphim/Dockerfile
                    build-args: |
                        PROJECT_BUILD_CONFIGURATION=Release
                    push: true
                    tags: ${{ env.REGISTRY_NAME }}.azurecr.io/seraphim-service:${{ github.sha }}
    deploy:
        runs-on: ubuntu-latest
        needs: build
        environment: dev
        steps:
            -   name: 'Authenticate with Azure'
                uses: 'azure/login@v1'
                with: 
                    creds: '${{ secrets.AZURE_CREDENTIALS }}'

            -   name: Deploy to Azure Web App
                id: deploy-to-webapp
                uses: azure/webapps-deploy@v2
                with:
                    app-name: dscrd-api-usce-as-dev
                    images: ${{ env.REGISTRY_NAME }}.azurecr.io/${{ env.REGISTRY_NAME }}/${{github.event.repository.name}}:${{ github.sha }}