name: Deploy Docker Based Web App

on:
  workflow_call:
    inputs:
      WEB_APP_NAME:
        required: true
        type: string
      ENVIRONMENT:
        required: true
        type: string

env:
  registryName: srphmusceacr

jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: build
    ENVIRONMENT: dev
    steps:
      - name: Authenticate with Azure
        uses: azure/login@v1
        with: 
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # - name: Deploy to Azure Web App
      #   uses: azure/webapps-deploy@v2
      #   with:
      #     app-name: ${{ inputs.WEB_APP_NAME }}
      #     images: ${{ env.registryName }}.azurecr.io/seraphim:${{ github.sha }}